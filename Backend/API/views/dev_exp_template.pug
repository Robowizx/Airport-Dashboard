//chart template
doctype html
html
    head
        script(src='https://localhost:4000/Javascript/apexcharts.min.js')
        link(rel='stylesheet',type='text/css',href='https://localhost:4000/CSS/apexcharts.min.js')
    body
        div(id='chart',style="max-width:device-width; margin: 35px 0px")
    script.
        var options = !{option};
        options.tooltip.y = {
            title:{
                formatter: function (seriesName){ return 'Experience Index till date' }
            }
        };
        var chart = new ApexCharts(document.querySelector("#chart"), options);
        chart.render();