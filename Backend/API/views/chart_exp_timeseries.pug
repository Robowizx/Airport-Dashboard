//chart template
doctype html
html
    head
        script(src='https://localhost:4000/Javascript/apexcharts.min.js')
        link(rel='stylesheet',type='text/css',href='https://localhost:4000/CSS/apexcharts.min.js')
    body
        - var ar = JSON.parse(area);
          each item in ar
            div(id=`${item}`,style="max-width:device-width; margin: 35px auto")
    script.
        var options = !{option};
        var areas = !{area};
        var series = !{series};
        for(i=0;i<areas.length;i++){
            options.title.text = `Experience Index Time Series of ${areas[i]}`
            let x = series.findIndex(a => a.name === areas[i]);
            options.series = [series[x]]        
            var chart = new ApexCharts(document.getElementById(`${areas[i]}`), options);
            chart.render();
        }
